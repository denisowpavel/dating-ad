import { SelectKeysPipe } from './select-keys.pipe';

describe('SelectKeysPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectKeysPipe();
    expect(pipe).toBeTruthy();
  });
});
